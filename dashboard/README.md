# Dashboard Frontend (React)

This folder is reserved for the React frontend, which will be integrated in a later stage.
It will serve as the visualization panel for live trading state, ORB levels, agent insights, and active order statuses.
